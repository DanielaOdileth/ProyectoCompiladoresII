PROGRAM Sort(input, output);
        {
            Multi
            Line
        }
    PROCEDURE ReadArr(VAR size: Integer; VAR a: IntArrType);
        BEGIN
            size := 1;
                IF NOT eof THEN
                  size := size + 1;
        END

    BEGIN
        { Read }
        ReadArr(size, arr);
    END.
